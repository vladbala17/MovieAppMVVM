<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="movie" type="com.android.vlad.movieapparchitecturecomponents.data.model.Movie"/>
        <variable name="callback" type="com.android.vlad.movieapparchitecturecomponents.view.movielist.MovieClickCallback"/>
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:onClick="@{() ->  callback.onClick(movie)}"
        android:layout_marginStart="2dp"
        android:layout_marginEnd="2dp"
        app:cardUseCompatPadding="true">

        <LinearLayout
            android:layout_marginStart="2dp"
            android:layout_marginEnd="2dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="5dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textStyle="bold"
                android:textSize="15sp"
                android:text="@{movie.title}"/>
        </LinearLayout>

    </androidx.cardview.widget.CardView>
</layout>